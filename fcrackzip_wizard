#!/bin/bash

clear

cowsay -f tux "Welcome to fcrackzip wizard."

r="\e[1;31m"
g="\e[1;32m"
b="\e[1;34m"
re="\e[0m"
w="\e[1;37m"

while true; do
    read -p "[$] Enter the zip file name: " zip
    if [[ -z ${zip} ]]; then
        echo -e "${r}[×] ${w}Error: the zip file cannot be empty.${re}"
    elif [[ -d ${zip} ]]; then
        echo -e "${r}[×] ${w}Error: '${file_zip}' is a directory not a file.${re}"
    elif [[ -f ${zip} && ${zip##*.} != "zip" ]]; then
        echo -e "${r}[×] ${w}Error: '${zip}' file is not a zip file.${re}"
    elif [[ ! -f ${zip} ]]; then
        echo -e "${r}[×] ${w}Error: '${zip}' file not found.${re}"
    else
        while true; do
            read -p "[$] Enter the wordlist file name: " wordlist 

            if [[ -z ${wordlist} ]]; then
                echo "[×] Error: the wordlist file cannot be empty."
            elif [[ -d ${wordlist} ]]; then
                echo "[×] Error: '${wordlist}' is a directory not a file."
            elif [[ ! -f ${wordlist} ]]; then
                echo "[×] Error: '${wordlist}' file not found."
            else
                echo ""
                echo "[*] Running fcrackzip..."
                echo ""
                password=$(fcrackzip -u -D -p ${wordlist} ${zip} | awk "NR == 3")
                if [[ -z $password ]]; then
                    echo "[×] PASSWORD NOT FOUND!!!!"
                    exit 0
                else
                    echo "[+] ${password}"
                    exit 0
                fi
                break
            fi
        done
        break
    fi
done
